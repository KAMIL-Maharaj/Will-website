@{
    ViewData["Title"] = "About Us";
}

@section Styles {
    <style>
        /* RESET & BASE */
        * { box-sizing: border-box; margin: 0; padding: 0; }
        html, body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: url('../images/Background4.jpg') center center / cover no-repeat fixed;
            color: #fff;
            min-height: 100vh;
            overflow-y: auto; /* allow scrolling */
        }

        a { text-decoration: none; color: inherit; }

        /* MAIN SECTION */
        .about-section {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 4rem 1rem;
        }

        /* GLASS CARD */
        .about-card {
            background: rgba(255, 255, 255, 0.15);
            border-radius: 2rem;
            padding: 2rem;
            backdrop-filter: blur(25px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 25px 60px rgba(0,0,0,0.5);
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
            max-width: 950px;
            width: 100%;
            align-items: center;
            animation: fadeIn 1.2s ease forwards;
        }

        .about-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 35px 80px rgba(0,0,0,0.6);
        }

        /* CARD TITLE */
        .about-card h1 {
            grid-column: 1 / -1;
            text-align: center;
            font-size: 3rem;
            color: #FFD700;
            margin-bottom: 1.5rem;
            text-shadow: 3px 3px 12px rgba(0,0,0,0.5);
            letter-spacing: 1px;
        }

        /* IMAGE GRID */
        .about-images {
            display: grid;
            gap: 1rem;
        }

        .about-images img {
            width: 100%;
            border-radius: 1rem;
            object-fit: cover;
            transition: transform 0.35s ease, box-shadow 0.3s ease;
        }

        .about-images img:first-child { height: 180px; }
        .about-images img:last-child { width: 180px; height: 180px; }
        .about-images img:hover {
            transform: scale(1.08) rotate(1deg);
            box-shadow: 0 15px 35px rgba(0,0,0,0.4);
        }

        /* CONTENT */
        .about-content h2 {
            font-size: 1.7rem;
            font-weight: 800;
            color: #fff;
            margin-bottom: 0.8rem;
            position: relative;
            text-shadow: 0 3px 8px rgba(0,0,0,0.5);
        }

        .about-content h2::after {
            content: "";
            width: 60px;
            height: 4px;
            background: linear-gradient(to right, #0072ff, #00c6ff);
            display: block;
            margin-top: 6px;
            border-radius: 2px;
        }

        .about-content p {
            font-size: 0.95rem;
            line-height: 1.6;
            color: #f1f1f1;
            margin-bottom: 1rem;
        }

        .about-content p.font-weight-bold {
            font-weight: 700;
            color: #fff;
        }

        /* RESPONSIVE */
        @@media (max-width: 992px) {
            .about-card {
                grid-template-columns: 1fr;
                text-align: center;
                padding: 1.8rem;
            }

            .about-content { margin-top: 1.5rem; }
            .about-images img:first-child { height: 140px; }
            .about-images img:last-child { width: 140px; height: 140px; margin: 0 auto; }
        }

        @@media (max-width: 768px) {
            .about-card h1 { font-size: 2.2rem; }
            .about-content h2 { font-size: 1.4rem; }
            .about-content p { font-size: 0.85rem; }
            .about-images img:first-child { height: 110px; }
            .about-images img:last-child { width: 110px; height: 110px; margin: 0 auto; }
        }

        /* ANIMATION */
        @@keyframes fadeIn {
            0% { opacity: 0; transform: translateY(20px); }
            100% { opacity: 1; transform: translateY(0); }
        }
    </style>
}

<!-- HTML -->
<div class="about-section">
    <div class="about-card">
        <h1>About Us</h1>
        <div class="about-images">
            <img src="~/images/team-member.jpg" alt="Team Praying Together">
            <img src="~/images/404.png" alt="Community Outreach">
        </div>
        <div class="about-content">
            <h2>Who We Are</h2>
            <p>Alabaster Box Anointing Ministries is a Christ-centered organization dedicated to spreading the love, hope, and healing power of Jesus Christ to communities near and far. Rooted in <strong>Mark 16:15</strong>, our mission is to go into all the world and share the Good News through outreach, prayer, charity, and discipleship.</p>
            <p>Founded with a heart for service and a passion for transformation, we believe that every person matters and that through faith, lives can be restored and empowered. Whether we’re feeding the hungry, praying for the brokenhearted, or equipping others with the Word of God, our purpose remains the same: to serve with compassion and walk in the anointing of the Holy Spirit.</p>
            <p class="font-weight-bold">Join us as we continue to pour out love like the alabaster jar — selflessly and wholeheartedly — for the glory of God.</p>
        </div>
    </div>
</div>
